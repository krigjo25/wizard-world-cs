namespace wizardWorld.lib.Items;

internal class Baton : Item
{
    public Baton(string name, string type,  int rarity = 0, string description = "") :
        base(name, type, description, rarity)
    {
        // Initialize something
    }
        
}